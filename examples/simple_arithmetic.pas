program arithmetic;
var a, b, sum, diff, prod: integer;

begin
    a := 20;
    b := 4;
    
    sum := a + b;
    diff := a - b;
    prod := a * b;
    
    writeln(sum);
    writeln(diff);
    writeln(prod)
end.
